<h2>Listing Types</h2>

<table class="table">
  <thead>
    <tr>
      <th>Code</th>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for code <- @codes do %>
    <tr>
      <td><%= code.code %></td>
      <td><%= code.name %></td>

      <td class="text-right">
        <span><%= link "Show", to: code_path(@conn, :show, code), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: code_path(@conn, :edit, code), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: code_path(@conn, :delete, code), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Type", to: code_path(@conn, :new) %></span>
