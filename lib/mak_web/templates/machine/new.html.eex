<h2><%= dgettext("forms", "new") %> <%= dgettext("forms", "machine")%></h2>

<%= render "form.html", Map.put(assigns, :action, machine_path(@conn, :create)) %>

<span><%= link dgettext("forms", "back"), to: machine_path(@conn, :index) %></span>
